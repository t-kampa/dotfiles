# VS Code Settings

Not managed by Stow — VS Code settings are project-dependent, so files here must be copied manually to the target location instead of symlinked.

## Global settings

- [settings.json](settings.json)

## Project settings

coming soon... Project-specific settings will live here too, named `<project>.settings.json` (e.g. `nextjs.settings.json`). Copy the relevant one into the project's `.vscode/settings.json` as needed.
